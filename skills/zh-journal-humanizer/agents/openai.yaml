interface:
  display_name: "ZH Journal Humanizer"
  short_description: "兼容旧写作入口；正式论文写作请使用总控 skill"
  default_prompt: "Use $zh-empirical-paper-writer for formal Chinese journal prose; $zh-journal-humanizer is a legacy wrapper."

policy:
  allow_implicit_invocation: false
